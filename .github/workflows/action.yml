name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "* * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: utaek/kendor@naver.com
        with:       
          CREDLY_USER: utaek
